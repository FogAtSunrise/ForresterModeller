<UserControl x:Class="ForresterModeller.src.Windows.Views.MathView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ForresterModeller.src.Windows.Views"
              xmlns:viewModels="clr-namespace:ForresterModeller.src.ProjectManager.WorkArea"
             d:DataContext="{d:DesignInstance viewModels:MatViewManager, IsDesignTimeCreatable=True}"
      HorizontalAlignment="Stretch"
             mc:Ignorable="d" 
           >
    <Grid Background="AliceBlue">
        <ListBox Name="itemsCtrl"  ItemsSource="{Binding Path=Models, Mode=TwoWay}" SelectedItem="{Binding Active, Mode=TwoWay,  UpdateSourceTrigger=PropertyChanged}" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Grid.IsSharedSizeScope="True"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" SharedSizeGroup="a"/>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                           
                        </Grid.ColumnDefinitions>

                        <Border Grid.Row="0"  Grid.Column="0"   BorderBrush="DimGray" BorderThickness="1">
                            <TextBlock Margin="5,0"  Text="{Binding NodeForMod.FullName}"  MaxWidth="200" TextWrapping="Wrap" />
                        </Border>
                  
                            <TextBlock Grid.Row="1"  Grid.Column="0"   Text="{Binding NodeForMod.Name}"  MaxWidth="200" TextWrapping="Wrap" > = </TextBlock>
                            <TextBlock Grid.Row="1"  Grid.Column="1"    Text="{Binding NodeForMod.Description}"  MaxWidth="200" TextWrapping="Wrap" />

                        <Grid Row="2"  Column="0"  >

                        </Grid>
                    </Grid>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
