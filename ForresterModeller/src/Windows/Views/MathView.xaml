<UserControl x:Class="ForresterModeller.src.Windows.Views.MathView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ForresterModeller.src.Windows.Views"
              xmlns:viewModels="clr-namespace:ForresterModeller.src.ProjectManager.WorkArea"
              xmlns:models="clr-namespace:ForresterModeller.src.Nodes.Models"
             d:DataContext="{d:DesignInstance viewModels:MatViewManager, IsDesignTimeCreatable=True}"
      HorizontalAlignment="Stretch"
             mc:Ignorable="d" 
           >
    <Grid Background="AliceBlue">
        <ListBox ItemsSource="{Binding Path=Diagram.GetAllNodes}" SelectedItem="{Binding ActiveOwnerItem, Mode=TwoWay,  UpdateSourceTrigger=PropertyChanged}" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Grid.IsSharedSizeScope="True"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.Resources>
                <DataTemplate DataType="{x:Type models:ConstantNodeViewModel}">
                    <Grid Margin="0,5">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" SharedSizeGroup="a"/>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                       

                        <Border Grid.Row="0"   BorderBrush="DimGray" BorderThickness="1">
                            <TextBlock Margin="5,0" FontSize="20"  FontWeight="Bold" Text="{Binding FullName}"  MaxWidth="200" TextWrapping="Wrap" />
                        </Border>
                        <TextBlock Grid.Row="1"   Text="Описание: "  FontWeight="Bold" />
                        <TextBlock  Grid.Row="2" Text="{Binding Description}"    />

                        <Grid Grid.Row="3">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />

                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="a"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"  Text="{Binding Name}" FontWeight="Bold">  =   </TextBlock>
                            <TextBlock  Grid.Row="0" Grid.Column="1"  Text="{Binding Value}"    />
                            <TextBlock Grid.Row="1" Grid.Column="0"  Text="{Binding Name}" FontWeight="Bold">  =</TextBlock>
                            <TextBlock  Grid.Row="1" Grid.Column="1"  Text="{Binding FullName}"    />

                        </Grid>
                        
                        
                        </Grid>
                </DataTemplate>
                
                
                <DataTemplate DataType="{x:Type models:LevelNodeModel}">
                    <Grid Margin="0,5">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" SharedSizeGroup="a"/>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"   BorderBrush="DimGray" BorderThickness="1">
                            <TextBlock Margin="5,0" FontSize="20"  FontWeight="Bold" Text="{Binding FullName}"  MaxWidth="200" TextWrapping="Wrap" />
                        </Border>
                        <TextBlock Grid.Row="1"   Text="Описание: "  FontWeight="Bold" />

                        <TextBlock  Grid.Row="2" Text="{Binding Description}"    />

                        <Grid Grid.Row="3">

                            <ItemsControl Name="it"  ItemsSource="{Binding Path=GetMathView, Mode=TwoWay}" >
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Grid.IsSharedSizeScope="True"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,5">


                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>


                                            <TextBlock Grid.Column="0" FontWeight="Bold"  Text="{Binding Left}"  />
                                            <TextBlock Grid.Column="2"    Text="{Binding Right}" HorizontalAlignment="Left"  />

                                        </Grid>

                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>



                        </Grid>

                    </Grid>

                </DataTemplate>
            </ListBox.Resources>
        </ListBox>

    </Grid>
</UserControl>
